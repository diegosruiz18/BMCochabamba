// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyCfoqpwDpkcoxp7SQVq_jg7aRBDQKG_w4o",
    authDomain: "bm-cochabamba.firebaseapp.com",
    databaseURL: "https://bm-cochabamba-default-rtdb.firebaseio.com",
    projectId: "bm-cochabamba",
    storageBucket: "bm-cochabamba.appspot.com",
    messagingSenderId: "290334330986",
    appId: "1:290334330986:web:a6ab22426be2da299afa13",
    measurementId: "G-SDPT66JNDC"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
